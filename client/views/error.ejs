<!DOCTYPE html>
<html lang="ru">

<head>
    <%- include('partials/header.ejs', { title: 'Страница не найдена - Книжная полка' }) %>
    <style>
        .error-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 70vh;
            text-align: center;
            padding: 40px 20px;
        }

        .error-code {
            font-size: 8rem;
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: 20px;
            line-height: 1;
        }

        .error-title {
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: var(--dark-color);
        }

        .error-message {
            font-size: 1.2rem;
            color: var(--gray-color);
            max-width: 600px;
            margin-bottom: 40px;
        }

        .error-image {
            max-width: 351px;
            margin-bottom: 40px;
        }

        @media (max-width: 768px) {
            .error-code {
                font-size: 5rem;
            }
            
            .error-title {
                font-size: 1.8rem;
            }
            
            .error-message {
                font-size: 1rem;
            }
            
            .error-image {
                max-width: 200px;
            }
        }
    </style>
</head>

<body>
    <%- include('partials/navbar', { activeLink: 'none', authButtons: !locals.user, user: locals.user }) %>

    <div class="container">
        <div class="error-container">
            <img src="/images/404-error.svg" alt="Страница не найдена" class="error-image">
            <h1 class="error-title">Страница не найдена</h1>
            <p class="error-message">
                Похоже, что страница, которую вы ищете, не существует или была перемещена.
                Попробуйте вернуться на главную страницу или воспользуйтесь поиском.
            </p>
            <a href="/" class="btn primary">
                <i class="fas fa-home"></i> Вернуться на главную
            </a>
        </div>
    </div>

    <%- include('partials/footer.ejs') %>
</body>

</html>